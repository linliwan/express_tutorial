<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title><%= title %></title>
</head>
<body>
    
    <header>
        <div class="logo">
            <img src="/images/logo.png" alt="logo">
        </div>
        <div class="nav">
            <ul>
                <li><a href="/">Home</a></li>
                    <% locals.tags && locals.tags.forEach(tag => { %>
                        <li><a href="/web/blogs/tags?tag=<%= tag.id %>"><%= tag.name %></a></li>
                    <% }); %>
            </ul>
        </div>
        <div class="search">
            <div class="admin">
                <a href="/web/admin">Admin</a>
            </div>
        </div>
    </header>

    <main>